<%= form_for(@taskitum) do |f| %>
  <% if @taskitum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taskitum.errors.count, "error") %> prohibited this taskitum from being saved:</h2>

      <ul>
      <% @taskitum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task %><br>
    <%= f.text_field :task %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
